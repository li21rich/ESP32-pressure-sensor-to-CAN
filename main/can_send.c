/**
* Author: Richard Li
* Editors: Richard Li
*/

/*
twai_message_t msg = {
    .identifier = 0x100,   // Brake pressure CAN ID
    .data_length_code = 2,
};
msg.data[0] = (adc_val >> 8) & 0xFF;
msg.data[1] = adc_val & 0xFF;
*/

//twai_transmit(&msg, pdMS_TO_TICKS(100));
